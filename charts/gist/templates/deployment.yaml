apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gist.fullname" . }}
  labels:
    app: {{ include "gist.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "gist.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "gist.name" . }}
    spec:
      containers:
        - name: {{ include "gist.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
