democratic-csi:
  driver: iscsi
  name: synology-iscsi
  controller:
    enabled: true
  node:
    enabled: true
  storageClasses:
    - name: synology-iscsi
      defaultStorageClass: true
      reclaimPolicy: Delete
      volumeBindingMode: Immediate
      allowVolumeExpansion: true
      parameters:
        fsType: ext4
        csi.storage.k8s.io/provisioner-secret-name: synology-iscsi-secret
        csi.storage.k8s.io/provisioner-secret-namespace: "{{ .Release.Namespace }}"
        csi.storage.k8s.io/node-stage-secret-name: synology-iscsi-secret
        csi.storage.k8s.io/node-stage-secret-namespace: "{{ .Release.Namespace }}"
  secrets:
    synology-iscsi-secret:
      host: "YOUR_SYNOLOGY_IP"
      port: "5000"
      username: "YOUR_USERNAME"
      password: "YOUR_PASSWORD"
      protocol: "http"
      apiVersion: "v1"
      # Add other required Synology API fields here
