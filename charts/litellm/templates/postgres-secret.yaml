apiVersion: v1
kind: Secret
metadata:
  name: {{ include "litellm.fullname" . }}-postgres-secret
  labels:
    app.kubernetes.io/name: {{ include "litellm.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
data:
  username: bGl0ZWxsbQ==  # litellm (base64)
  password: bGl0ZWxsbXBhc3N3b3Jk  # litellmpassword (base64)
